en:
  port_cost_calculator:
    errors:
      no_place_available: 'Oooops, it looks like there is no place available for your yacht'
  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              immutable: "You can't change your email address"
  messages:
    wrong_size: "is the wrong size (should be %{file_size})"
    size_too_small: "is too small (should be at least %{file_size})"
    size_too_big: "is too big (should be at most %{file_size})"
